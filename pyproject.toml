[project]
name = "sales-analytics-dashboard"
version = "0.1.0"
description = "Interactive sales analytics dashboard for ShopSmart"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.28.0",
    "pandas>=2.0.0",
    "plotly>=5.18.0",
]

[project.optional-dependencies]
dev = [
    "ruff>=0.1.0",
]
